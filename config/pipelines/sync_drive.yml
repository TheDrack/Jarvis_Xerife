# JARVIS Pipeline: Sincronização e Backup de DNA
components:
  consolidator:
    id: "consolidator"
    hint_path: "app/adapters/infrastructure"

  drive_uploader:
    id: "drive_uploader"
    hint_path: "app/adapters/infrastructure"
    singleton: true
    # Se o Google Drive falhar (cota), o pipeline CONTINUA para o Telegram/Gist
    config:
      strict_mode: false 

  telegram_backup:
    id: "telegram_adapter"
    hint_path: "app/adapters/infrastructure"
    config:
      action: "upload_backup"
      token: "${{ secrets.TELEGRAM_TOKEN }}"
      chat_id: "${{ secrets.TELEGRAM_CHAT_ID }}"

  gist_backup:
    id: "gist_uploader"
    hint_path: "app/adapters/infrastructure"
