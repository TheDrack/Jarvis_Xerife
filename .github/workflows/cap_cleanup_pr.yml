name: Architecture & Capability Governance

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Modo de execução"
        required: true
        default: "govern"
        type: choice
        options:
          - audit
          - govern

jobs:
  governance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependências mínimas
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Rodar validação e governança (com PYTHONPATH correto)
        env:
          MODE: ${{ inputs.mode }}
          PYTHONPATH: ${{ github.workspace }}
        run: |
          python scripts/architecture_and_cap_governance.py

      - name: Upload relatório de caps mortos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dead-caps-report
          path: dead_caps.txt

      - name: Criar Pull Request de governança
        if: inputs.mode == 'govern'
        uses: peter-evans/create-pull-request@v6
        with:
          base: main
          branch: governance/capabilities
          title: "Architecture governance: capability lifecycle sync"
          body: |
            Governança automática de capabilities:

            - 🧊 Congela caps mortos
            - 🔥 Ressuscita caps usados
            - 🧠 Análise Nexus-aware
            - 🚫 Core, Scripts e infra ignorados

            Gerado automaticamente.
          commit-message: "chore(governance): sync capability lifecycle"