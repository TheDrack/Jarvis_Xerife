name: Auto Evolution Trigger

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  evolution_cycle:
    name: "🧬 Ciclo Unificado JARVIS"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Essencial para ler o histórico de commits

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Instalar Dependências
        run: pip install sqlmodel pydantic requests

      - name: 🧠 Orquestrador de Fluxo
        id: flow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}$(pwd)"
          python - << 'PYCODE'
          import os, sys, re
          try:
              from app.application.services.auto_evolutionV2 import AutoEvolutionServiceV2
              auto = AutoEvolutionServiceV2()
              
              # Obtemos a mensagem completa do commit (incluindo o corpo) e o autor
              last_msg = os.popen("git log -1 --pretty=%B").read()
              last_auth = os.popen("git log -1 --pretty=%an").read()
              
              print(f"--- DEBUG INFO ---")
              print(f"Author: {last_auth.strip()}")
              print(f"Message: {last_msg.strip()}")
              print(f"------------------")

              # 1. IDENTIFICAÇÃO DE ESTADOS (BUSCA MULTILINE)
              # Verifica se a tag existe em qualquer parte da mensagem
              is_auto = "[Auto-Evolution]" in last_msg or "Jarvis-AutoEvolution" in last_auth
              # Verifica se é o commit de sincronização do JSON
              is_sync = "Inventory Synchronized" in last_msg

              # CASO A: Commit de Sincronização (Auto-bloqueio)
              if is_sync:
                  print("🛡️ Trava Anti-Loop: O inventário já foi atualizado. Parando.")
                  with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                      f.write("action=idle\n")
                  sys.exit(0)

              # CASO B: É um Merge ou Push que contém código de evolução
              elif is_auto and not is_sync:
                  print("🎯 Identificado commit de evolução. Iniciando sincronização do JSON...")
                  # Procura o ID da missão (CAP-XXX) em toda a mensagem
                  match = re.search(r'CAP-\d+', last_msg)
                  
                  if match:
                      cap_id = match.group()
                      print(f"✅ Atualizando status da missão {cap_id} para 'complete'...")
                      if auto.mark_mission_as_completed(cap_id):
                          os.system("git config --global user.name 'Jarvis-AutoEvolution'")
                          os.system("git config --global user.email 'jarvis@bot.com'")
                          os.system("git add data/capabilities.json")
                          # A mensagem abaixo garante que o PRÓXIMO trigger caia no CASO A
                          os.system(f"git commit -m '[Auto-Evolution] Inventory Synchronized ✅ for {cap_id}' || echo 'No changes'")
                          os.system("git push origin main")
                  
                  with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                      f.write("action=sync_only\n")
                  sys.exit(0)

              # CASO C: Commit humano ou Merge de outra feature
              else:
                  print("🚀 Detectado commit humano. Verificando se há missões pendentes...")
                  next_m = auto.find_next_mission()
                  if next_m:
                      cap_id = next_m['id']
                      # Verifica se já não existe uma PR aberta para este CAP-ID
                      pr_check = os.popen(f'gh pr list --search "{cap_id}" --state open --json title').read()
                      
                      if len(pr_check.strip()) > 5:
                          print(f"⚠️ Já existe uma PR aberta para {cap_id}. Aguardando merge.")
                          action = "wait"
                      else:
                          with open('mission_context.txt', 'w', encoding='utf-8') as f:
                              f.write(auto.get_roadmap_context(next_m))
                          action = "evolve"
                          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                              f.write(f"mission_desc={cap_id}: {next_m['title']}\n")
                  else:
                      print("✅ Tudo em dia. Nenhuma missão pendente.")
                      action = "idle"
                  
                  with open(os
