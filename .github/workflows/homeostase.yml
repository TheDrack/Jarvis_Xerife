name: "🧬 JARVIS Self Healing V3"

on:
  pull_request:
    branches: [main, master, develop]
  workflow_run:
    workflows: ["*"]
    types: [completed]
  workflow_dispatch:

jobs:
  homeostasis:
    name: "🧪 Vistoria e Auto-Cura"
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'pull_request' || 
      (github.event_name == 'workflow_run' && 
       github.event.workflow_run.conclusion == 'failure' && 
       github.event.workflow_run.name != '🧬 JARVIS Self Healing V3')
    
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GIT_PAT }}

      - name: 📦 Setup UV
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: '3.11'

      - name: 🔧 Instalação do DNA
        run: |
          uv venv --clear --python 3.11
          uv pip install pytest pytest-json-report groq requests autopep8 python-dotenv

      - name: 🔍 Modo de Recuperação (Workflow Failure)
        if: github.event_name == 'workflow_run'
        id: recover_logic
        run: |
          # 1. Identifica o commit que falhou e o pai dele (estável)
          FAILED_SHA=${{ github.event.workflow_run.head_sha }}
          STABLE_SHA=$(git rev-parse $FAILED_SHA^)
          BACKUP_BRANCH="jarvis/broken-state-${{ github.run_id }}"
          RECOVERY_BRANCH="jarvis/recovery-${{ github.run_id }}"

          # 2. Cria branch de backup do erro para análise posterior
          git branch $BACKUP_BRANCH $FAILED_SHA
          git push origin refs/heads/$BACKUP_BRANCH

          # 3. Prepara a branch de recuperação baseada no commit estável
          git checkout -b $RECOVERY_BRANCH $STABLE_SHA
          
          echo "target_branch=$RECOVERY_BRANCH" >> $GITHUB_OUTPUT
          echo "backup_branch=$BACKUP_BRANCH" >> $GITHUB_OUTPUT

      - name: 🔄 Ciclo de Auto-Cura e Testes
        id: healing_process
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          PYTHONPATH: "."
        run: |
          # Execução do Healer (Tenta corrigir o que estiver no diretório atual)
          uv run pytest --json-report --json-report-file=report.json > pytest_log.txt 2>&1 || true
          uv run python scripts/jarvis_healer.py --report report.json --log pytest_log.txt
          
          # Validação Final
          uv run pytest --json-report --json-report-file=report_final.json > final_log.txt 2>&1 || true
          
          if ! grep -qE "ERRORS|failed|IndentationError" final_log.txt; then
             echo "✅ Integridade validada."
             echo "status=success" >> $GITHUB_OUTPUT
          else
             echo "❌ DNA instável."
             echo "status=failure" >> $GITHUB_OUTPUT
             exit 1
          fi

      - name: 🛠️ Commit & Merge (PR Mode)
        if: github.event_name == 'pull_request' && steps.healing_process.outputs.status == 'success'
        env:
          GH_TOKEN: ${{ secrets.GIT_PAT }}
        run: |
          git config user.name "JARVIS"
          git config user.email "jarvis@simbiose.ai"
          git add .
          
          if ! git diff --staged --quiet; then
            git commit -m "chore: auto-cura integridade"
            # Solução do Erro: Usar o caminho completo da ref
            git push origin HEAD:refs/heads/${{ github.head_ref }}
            
            gh pr merge ${{ github.event.pull_request.number }} --auto --merge || \
            gh pr merge ${{ github.event.pull_request.number }} --merge --admin
          else
            echo "Nenhuma alteração pendente."
          fi

      - name: 🚀 Abrir PR de Reparo (Recovery Mode)
        if: github.event_name == 'workflow_run' && steps.healing_process.outputs.status == 'success'
        env:
          GH_TOKEN: ${{ secrets.GIT_PAT }}
        run: |
          git config user.name "JARVIS"
          git config user.email "jarvis@simbiose.ai"
          git add .
          
          if ! git diff --staged --quiet; then
            git commit -m "fix: restauração estável e correção automática"
            # Push garantindo o refname completo
            git push origin HEAD:refs/heads/${{ steps.recover_logic.outputs.target_branch }}
            
            PR_URL=$(gh pr create --title "JARVIS Recuperação: ${{ github.event.workflow_run.name }}" \
              --body "### 🛡️ Sistema de Auto-Cura Ativado
              - **Causa:** Falha no workflow [${{ github.event.workflow_run.name }}]
              - **Backup do Erro:** \`${{ steps.recover_logic.outputs.backup_branch }}\`
              - **Ação:** Reversão para commit estável e aplicação de patches corretivos." \
              --head ${{ steps.recover_logic.outputs.target_branch }} \
              --base main)
              
            gh pr merge $PR_URL --auto --merge || gh pr merge $PR_URL --admin --merge
          fi
