name: "🧬 JARVIS: Self Healing V3"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  # Permite rodar manualmente no botão "Run workflow"
  workflow_dispatch: 
  # Tenta ouvir outros workflows
  workflow_run:
    workflows: ["*"]
    types: [completed]

jobs:
  homeostasis:
    name: "🧪 Vistoria e Auto-Cura"
    # Só não roda se o commit for da própria auto-cura
    if: "!contains(github.event.head_commit.message, '[Auto-Cura]')"
    runs-on: ubuntu-latest

    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GIT_PAT }}

      - name: 📦 Setup UV
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: '3.11'

      - name: 🔧 Instalação Flash do DNA
        run: |
          uv venv --clear --python 3.11
          # Instalação em bloco único para evitar conflitos de versão
          uv pip install pytest pytest-json-report pytest-cov sqlmodel groq requests python-dotenv \
                         fastapi[all] pydantic-settings cryptography python-jose[cryptography] \
                         pandas numpy scikit-learn pytest-asyncio pyttsx3 pyautogui pyperclip \
                         httpx python-multipart passlib[bcrypt] bcrypt
          echo "PYTHONPATH=$PYTHONPATH:$(pwd)" >> $GITHUB_ENV

      - name: 🔄 Ciclo de Execução
        id: healing_process
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          PYTHONPATH: "."
        run: |
          uv run pytest --json-report --json-report-file=report.json || true
          
          if [ -f report.json ]; then
            # Puxa falhas, erros de coleta e itens na lista de erros explicitamente
            FAILED=$(jq '.summary.failed // 0' report.json)
            COLLECT_ERRORS=$(jq '.summary.errors // 0' report.json)
            # Verifica se a lista de erros não está vazia
            LIST_ERRORS=$(jq '.errors | length' report.json)
            
            TOTAL=$((FAILED + COLLECT_ERRORS + LIST_ERRORS))
            
            if [ "$TOTAL" -gt 0 ]; then
              echo "🚨 Problemas reais detectados ($TOTAL). Ativando Healer..."
              uv run python scripts/jarvis_healer.py --report report.json
              
              echo "🔄 Revalidando..."
              uv run pytest --json-report --json-report-file=report_final.json || true
              FINAL_TOTAL=$(jq '(.summary.failed // 0) + (.summary.errors // 0) + (.errors | length)' report_final.json)
              
              if [ "$FINAL_TOTAL" -eq 0 ]; then
                echo "final_status=true" >> $GITHUB_OUTPUT
                echo "healed=true" >> $GITHUB_OUTPUT
              else
                echo "final_status=false" >> $GITHUB_OUTPUT
              fi
            else
              echo "✅ DNA Íntegro."
              echo "final_status=true" >> $GITHUB_OUTPUT
            fi
          else
            echo "❌ Erro: report.json não gerado."
            exit 1
          fi

      - name: 🧬 Commit Auto-Cura
        if: steps.healing_process.outputs.healed == 'true'
        run: |
          git config --local user.email "jarvis@simbiose.ai"
          git config --local user.name "🧬 JARVIS Homeostase"
          git add .
          git commit -m "[Auto-Cura] Correção automática de DNA"
          git push

      - name: 🚨 Falha Final
        if: steps.healing_process.outputs.final_status == 'false'
        run: exit 1
