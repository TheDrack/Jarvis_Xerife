name: "💎 JARVIS: Crystallizer"

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'data/capabilities.json'
      - 'app/domain/**'
      - 'app/application/services/**'

jobs:
  organize:
    name: "💎 Ciclo de Cristalização DNA"
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIT_PAT }}
          fetch-depth: 0

      - name: 📦 Setup UV
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: '3.11'

      - name: 🏗️ Prepare Environment & Structural Crystallization
        run: |
          uv venv --clear --python 3.11
          # Adicionado libcst para evitar o ModuleNotFoundError
          uv pip install requests sqlmodel pydantic python-dotenv libcst
          
          echo "🛠️ Rodando Sanidade Estrutural (__init__.py e Classes)..."
          # Garantindo que o diretório atual está no path
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          uv run python scripts/cristalize_project.py

      - name: 🧠 Run Crystallizer Engine (Full Cycle)
        env:
          PYTHONPATH: ${{ github.workspace }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: |
          uv run python - << 'PYCODE'
          import sys
          import os
          sys.path.append(os.getcwd())
          
          from app.application.services.crystallization.crystallizer_engine import CrystallizerEngine
          
          try:
              print("💎 Iniciando Full Cycle do Crystallizer Engine...")
              engine = CrystallizerEngine()
              engine.run_full_cycle()
              print("✨ Cristalização lógica concluída.")
          except Exception as e:
              print(f"❌ Erro: {e}")
              sys.exit(1)
          PYCODE
          
      - name: 🔄 Sync DNA Map & Containers
        env:
          TOKEN: ${{ secrets.GIT_PAT }}
        run: |
          git config --global user.name "Jarvis-Crystallizer"
          git config --global user.email "jarvis@bot.com"
          
          git remote set-url origin https://x-access-token:${TOKEN}@github.com/${{ github.repository }}.git
          
          git add .
          
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "chore: [Crystallizer] DNA Mapping & Structural Synchronization [Auto-Cura]"
            git pull origin main --rebase
            git push origin main
          else
            echo "✅ DNA e Estrutura já estão sincronizados e estáveis."
          fi
